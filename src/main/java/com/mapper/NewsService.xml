<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.service.NewsService">
    <!--namespace :对应的接口类-->
    <!--select :对应的接口类的方法-->
    <!--parameterType :对应的接口类的参数类型-->
    <!--resultType :对应的接口类的返回类型-->
    <select id="selectNews" resultType="com.bean.News">
        SELECT * FROM account
    </select>
    <select id="selectNewsById" parameterType="java.lang.Long" resultType="com.bean.News">
        SELECT * FROM account WHERE newId=#{newId}
    </select>
    <insert id="addNews" parameterType="com.bean.News">
        INSERT INTO news (title,content,category,author) VALUES (#{title},#{content},#{category},#{author})
    </insert>

    <update id="editNews" parameterType="com.bean.News">
        UPDATE users seSETt title=#{title},acontentge=#{content} ,category=#{category} ,author=#{author} where newId=#{newId}
    </update>
</mapper>